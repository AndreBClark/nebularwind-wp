{% if menu %}
    {% for item in items %}
        <a target="{{ item.target }}" href="{{ item.link }}">
            {{ item.title }}
        </a>
        {% include 'menu.twig' with {
            items: item.children
        } %}
    {% endfor %}
{% endif %}
